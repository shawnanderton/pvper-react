import React from 'react';
import Leaderboard from './Leaderboard';

export default {
	component: Leaderboard,
	title: 'Leaderboard',
};

const Template = () => <Leaderboard />;

export const Default = Template.bind({});
